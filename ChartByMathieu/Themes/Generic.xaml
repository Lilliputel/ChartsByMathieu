<ResourceDictionary	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:local="clr-namespace:ChartByMathieu">

	<local:DebugConverter x:Key="Debugger"/>

	<Style TargetType="{x:Type local:Bar}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:Bar}">
					<Grid>
						<Grid.LayoutTransform>
							<RotateTransform Angle="{TemplateBinding Angle}"/>
						</Grid.LayoutTransform>
						<Border BorderBrush="{TemplateBinding BorderBrush}"
								BorderThickness="{TemplateBinding BorderThickness}"
								
								Background="{TemplateBinding BarColor}"
								CornerRadius="{TemplateBinding CornerRadius}"
								
								Width="{TemplateBinding EndValue}">
							
							<Border.Margin>
								<Thickness Top="0" Bottom="0" Right="0" Left="{TemplateBinding StartValue}"/>
							</Border.Margin>
						</Border>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="{x:Type local:Card}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:Card}">
					<Border Background="{TemplateBinding Background}"
							BorderBrush="{TemplateBinding BorderBrush}"
							BorderThickness="{TemplateBinding BorderThickness}"
							CornerRadius="{TemplateBinding CornerRadius}">
						<ContentPresenter x:Name="presenter" 
										  HorizontalAlignment="Center" 
										  VerticalAlignment="Center"/>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsDetailed" Value="True">
							<Setter TargetName="presenter" Property="Content" 
								Value="{Binding Back, RelativeSource={RelativeSource TemplatedParent}}"/>
							<Setter TargetName="presenter" Property="ContentTemplate"
								Value="{Binding BackTemplate, RelativeSource={RelativeSource TemplatedParent}}"/>
						</Trigger>

						<Trigger Property="IsDetailed" Value="False">
							<Setter TargetName="presenter" Property="Content" 
								Value="{Binding Front, RelativeSource={RelativeSource TemplatedParent}}"/>
							<Setter TargetName="presenter" Property="ContentTemplate"
								Value="{Binding FrontTemplate, RelativeSource={RelativeSource TemplatedParent}}"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	
	

	<Style TargetType="{x:Type local:Chart}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:Chart}">
					<Border Background="{TemplateBinding Background}"
							BorderBrush="{TemplateBinding BorderBrush}"
							BorderThickness="{TemplateBinding BorderThickness}">
					</Border>
				</ControlTemplate> 
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="{x:Type local:GanttChart}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:GanttChart}">
					<Border Background="{TemplateBinding Background}"
							BorderBrush="{TemplateBinding BorderBrush}"
							BorderThickness="{TemplateBinding BorderThickness}">
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
